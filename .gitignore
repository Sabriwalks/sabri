node_modules/
.env
.DS_Store

# Capacitor's bundled web assets — regenerated from the real source files
# (index.html, app.js, style.css, etc. at repo root) via `npm run
# build:capacitor` before `npx cap sync`. Never edit files in here directly.
www/

.vercel
.env*

# Generated audio from scripts/tts-comparison.js — a one-off voice-quality
# test, never meant to be committed.
/tts-comparison-output/
